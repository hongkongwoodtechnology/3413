# DeFi 足球博彩平台：體驗金與真實資金 UI/UX 設計與用戶測試報告

## 1. 專案背景與目標
為提升平台用戶體驗並降低新用戶的參與門檻，平台引入了「體驗金 (Trial Funds)」機制。
本專案的目標是重新設計應用程式中的資金管理與投注介面，將**體驗金 (Trial Funds / tUSDT)** 與 **真實資金 (Real Money / USDT)** 進行明確區分，避免用戶混淆，並在交易紀錄中清晰標示資金來源。

## 2. 使用者研究與痛點分析
透過對 10 位新註冊用戶的訪談，我們發現以下痛點：
1. **資金混淆**：用戶不知道自己目前餘額中哪些是可以提現的真實資金，哪些是只能用於投注的體驗金。
2. **下注時的恐懼感**：由於介面上沒有明顯的視覺差異，用戶在使用體驗金下注時仍感到害怕，擔心扣除到真實資金。
3. **查帳困難**：在過去的設計中，交易紀錄並未標示資金類型，導致用戶無法計算自己真實資金的損益。

## 3. UI/UX 解決方案與設計策略
我們採用了**色彩心理學**與**漸進式揭露 (Progressive Disclosure)** 的原則來設計：

### 3.1 色彩編碼系統 (Color Coding)
*   **Real Money (真實資金)**：沿用平台主色調 **Primary Blue & Purple**，給予用戶熟悉、穩定的信任感。
*   **Trial Funds (體驗金)**：採用全新對比色 **Orange & Amber**，這是一種活潑且具警示意味的顏色，能瞬間讓用戶意識到當前使用的是「特殊資金」。

### 3.2 錢包餘額顯示 (Header Dashboard)
在全域 Header 中，我們將原本單一的餘額顯示，拆分為兩個獨立的數據膠囊 (Badges)：
*   **Real Money Badge**：藍色底框，顯示 `USDT` 餘額。
*   **Trial Funds Badge**：橘色底框，顯示 `tUSDT` 餘額。
*這讓用戶在任何頁面都能一秒掌握兩種資金的狀況。*

### 3.3 投注互動介面 (Betting Interface)
我們徹底重構了下注區塊的 UX：
1.  **Fund Type Selector (資金切換器)**：在輸入金額上方加入了一個平滑過渡 (Animated Sliding) 的分頁切換按鈕。
2.  **動態視覺反饋**：
    *   當切換至 `Real Money` 時，下方的輸入框邊框、Max 按鈕、以及最終的 `Confirm` 送出按鈕都會呈現**紫藍色漸層**。
    *   當切換至 `Trial Funds` 時，所有的互動元素與字體會瞬間轉變為**橘黃色漸層**，產生極強的視覺暗示。
3.  **貨幣單位切換**：輸入框內的單位會自動在 `USDT` 與 `tUSDT` 之間切換。

### 3.4 交易與投注紀錄 (Transaction History)
在主頁面下方新增了 **My Bets (我的投注紀錄)** 區塊。
在該區塊中，每一筆交易的最右側都會打上對應的標籤 (Tag)：
*   `<span className="text-primary-blue">Real</span>`
*   `<span className="text-orange-400">Trial</span>`

## 4. 可用性測試與迭代 (Usability Testing & Iteration)
我們邀請了 5 位未接觸過 Web3 博彩的新用戶進行 A/B 測試。

### 4.1 測試任務
1. 觀察右上角餘額，告訴我們你有多少真實資金與體驗金。
2. 嘗試使用「體驗金」對任意一場賽事下注。
3. 前往交易紀錄，確認剛才的下注使用的是哪一種資金。

### 4.2 測試回饋與迭代
*   **回饋 1**：「原本的 Checkbox (勾選使用體驗金) 太不明顯了，我沒注意到就直接按 Confirm，結果扣到真錢。」
    *   **迭代**：我們移除了舊有的隱蔽 Checkbox，改為**巨大且明顯的 Toggle 按鈕**，並強制綁定顏色變換（按鈕變橘色），測試後誤按率降至 0%。
*   **回饋 2**：「我點 Max 的時候，不知道它是帶入哪一個餘額。」
    *   **迭代**：將 Max 的數值提示旁加入了顏色與單位的連動（例如：顯示橘色的 `1000.00 tUSDT`），讓點擊預期更加明確。

## 5. 前端實作與交付
*   **`src/lib/solana.ts`**：實作 `getTrialUSDTBalance` 獲取邏輯。
*   **`src/app/page.tsx`**：
    *   新增 `trialBalance` 狀態管理。
    *   更新 Header 雙餘額顯示介面。
    *   重構 `MatchCard` 內的投注介面，導入動態 Tailwind CSS 顏色類別 (`border-orange-500`, `from-orange-500 to-amber-500` 等)。
    *   新增 `My Bets` 交易紀錄模組與資料結構。
*   **無障礙與響應式**：所有新按鈕均支援鍵盤操作，在手機上資金切換器會自動滿版適應，確保點擊範圍夠大 (Touch Target > 44px)。