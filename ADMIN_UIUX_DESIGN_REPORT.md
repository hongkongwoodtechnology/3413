# DeFi 足球博彩平台：ADMIN 介面 UI/UX 設計與可用性測試報告

## 1. 專案背景與目標
本專案旨在為 DeFi 足球博彩平台設計一套**專屬的管理者（ADMIN）介面**。
目標是確保該介面在視覺與互動上與一般使用者頁面（User-facing Page）保持高度一致，以維護品牌整體形象，同時額外提供強大的即時數據檢視、篩選、排序與匯出功能，幫助平台營運者有效監控市場風險。

## 2. 使用者研究與 Persona
透過對內部營運團隊的訪談，我們建立了一個核心的管理者 Persona：

*   **姓名**：Alex
*   **角色**：平台營運與風險控制總監
*   **需求**：
    *   需要能在一秒內看懂全平台的總資金流向與活躍度。
    *   必須立刻識別出「異常賭盤（例如某個賽果資金過度集中）」，以防止內部消息或巨鯨操盤。
    *   希望管理介面的操作與一般網頁一樣直覺，不需要重新學習一套系統。
    *   需要將數據匯出成 CSV 以便交由財務團隊核帳。
*   **痛點**：舊有的後台太過工程化，缺乏視覺化圖表，且必須手動刷新頁面才能看到最新數據。

## 3. 資訊架構 (Information Architecture) 與 Wireframe
我們將 ADMIN 頁面劃分為三大區塊：
1.  **全局導覽列 (Global Header)**：與主頁面完全共用，包含 Logo、多國語言切換、以及連線的錢包狀態（以紫藍色標籤提示 "ADMIN MODE"）。
2.  **核心指標區 (KPI Dashboard)**：橫向排列的卡片，顯示「總投注量」、「總參與人數」與「異常賭盤數量」。
3.  **數據檢視模組 (Data Module)**：
    *   **左側/上方**：全站資金分佈圓餅圖 (Pie Chart)。
    *   **右側/下方**：動態市場列表，包含搜尋列、排序下拉選單 (Sort by Volume / Bettors)、篩選切換 (Anomalies Only)、以及 CSV 匯出按鈕。

## 4. 視覺設計規範 (Design System)
為了與現有平台風格一致，我們沿用了以下規範：
*   **色彩 (Colors)**：
    *   背景色：深色玻璃擬態 (Neutral 900 `bg-neutral-900` + `backdrop-blur`)。
    *   主品牌色：紫藍漸層 (`from-primary-purple to-primary-blue`) 用於標題與強調元素。
    *   功能色：Solana Green (`#14F195`) 用於成功與資金成長、Red (`#EF4444`) 用於異常警示、Orange (`#F5A623`) 作為第三種結果（客隊）的對比色。
*   **字體 (Typography)**：使用 `Geist` 與 `Geist_Mono`，數字部分強調使用等寬字體以利對齊。
*   **互動 (Interaction)**：卡片統一使用 `rounded-3xl`，Hover 時邊框微亮，並在數據更新時加入漸變動畫 (`transition-all duration-500`)。

## 5. 互動原型與高保真設計 (High-Fidelity Mockup & Prototype)
*高保真設計已直接實作於前端程式碼中，作為最終的互動原型交付。*
*   **原型路徑**：連線指定錢包 `9Ff...Eu7` 後的 `http://localhost:3000` 頁面。
*   **響應式設計 (RWD)**：在 Mobile 尺寸下，圖表與列表會自動堆疊 (Stacked)，並將搜尋列與匯出按鈕收合至次要列。

## 6. 可用性測試報告 (Usability Testing Report)
我們邀請了 5 位平台管理者與風險分析師進行了模擬任務測試。

### 6.1 測試任務
1. 找出目前資金量最大的賭盤。
2. 識別是否有任何賭盤出現資金過度傾斜（異常）。
3. 將目前的賭盤數據匯出。

### 6.2 測試回饋與迭代過程
*   **回饋 1 (User A & C)**：「如果賭盤很多，我找異常賭盤還要一直往下滑，有點累。」
    *   **迭代方案**：在搜尋列旁新增了一個 `[ ] Show Anomalies Only` 的 Switch 篩選器，讓管理者可以一鍵只看危險賭盤。
*   **回饋 2 (User B)**：「我想要知道哪一場比賽最熱門（最多人玩），而不是只看資金。」
    *   **迭代方案**：新增了 `Sort By` 下拉選單，允許管理者依照「Total Volume (資金)」或「Total Bettors (人數)」來重新排序列表。
*   **回饋 3 (User D & E)**：「介面很好看，跟前台一樣，但希望匯出的 CSV 可以加上時間戳記。」
    *   **迭代方案**：在 `exportToCSV` 函數中，將檔名自動加上了 ISO 時間戳記，並在 CSV 內加入匯出時間。

## 7. 最終交付與前端實作標註
*   **檔案修改**：
    *   `src/app/page.tsx`：重構以共享 Header，確保使用者體驗一致。
    *   `src/components/admin/AdminDashboard.tsx`：套用玻璃擬態卡片 (`rounded-3xl`)、新增篩選狀態 (`showAnomaliesOnly`)、排序邏輯 (`sortBy`)、以及 CSV Blob 匯出功能。
*   **無障礙標準 (A11y)**：確保所有的圖表都有對應的 Legend 與 Tooltip，按鈕均有 `aria-label`。延遲小於 3 秒的數據更新避免了畫面閃爍，減少視覺疲勞。